---
# Download OpenTelemetry Collector packages for Linux
- name: Set package type based on OS family
  set_fact:
    pkg_ext: "{{ 'rpm' if ansible_os_family == 'RedHat' else 'deb' }}"

- name: Download otelcol-contrib package
  get_url:
    url: "https://github.com/open-telemetry/opentelemetry-collector-releases/releases/download/v{{ otelcol_contrib_version }}/otelcol-contrib_{{ otelcol_contrib_version }}_{{ ansible_architecture }}.{{ pkg_ext }}"
    dest: "/tmp/otelcol-contrib-{{ otelcol_contrib_version }}.{{ pkg_ext }}"
    mode: 644
    timeout: 60
    validate_certs: yes
  register: download_result
  retries: 3
  delay: 5
  until: download_result is succeeded
