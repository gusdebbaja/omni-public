# inventory/hosts.yml
all:
  children:
    linux:
      hosts:
        redhat1:
          ansible_host: 172.22.149.98
          ansible_user: ansible
          ansible_password: "{{ linux_ansible_password }}"
    windows:
      hosts:
        windows1:
          ansible_host: 172.22.154.43
          ansible_user: ansible
          ansible_password: "{{ windows_ansible_password }}"
          ansible_winrm_server_cert_validation: ignore
    sandbox:
      hosts:
        windows:
        redhat:
    app_a:
      hosts:
    sql_servers:
      hosts:
    production:
      hosts:
